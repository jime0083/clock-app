# Problem - 発見された問題

---

## Problem 8: FCM認証エラーが解消されない（IAM権限設定後も継続）(2026-04-12) [解決済み 2026-04-13]

### 根本原因
**開発用APNs認証キーがFirebase Consoleに設定されていなかった**

- Xcodeからビルドしたアプリは開発環境（APNs Sandbox）で動作する
- 本番用APNs認証キーのみ設定されており、開発用が未設定だった
- そのため、FCMがAPNsにメッセージを送信できなかった

### 解決策
Firebase Console > Cloud Messaging > Apple アプリの構成 で、開発用APNs認証キーをアップロード
- 本番用と同じ.p8ファイル（AuthKey_YY7USJ7P4T.p8）を開発用としてもアップロード
- Key ID: YY7USJ7P4T
- Team ID: GK79P6R4DF

### 学び
- APNs認証キー（.p8）は開発・本番共通で使用可能だが、Firebase Consoleでは開発用・本番用の両方にアップロードが必要
- IAMロールやAPI有効化は全て正しく設定されていた
- 問題はAPNs認証キーの設定漏れだった

---

## Problem 9: バックグラウンド・ロック画面でアプリのアラーム音が鳴らない (2026-04-13)

### 症状
- **アプリ起動時（フォアグラウンド）**: アラーム音が鳴る ✓
- **ホーム画面（バックグラウンド）**: 通知音は鳴るがアプリのアラーム音が鳴らない ✗
- **ロック画面**: 通知音は鳴るがアプリのアラーム音が鳴らない ✗

### 重要な区別
- **通知音**: FCM/APNsが再生する短い音（"default" = 約2秒）→ **動作している** ✓
- **アラーム音**: `audioService.playAlarmSound()`が再生するループ音（アラーム音.m4a）→ **バックグラウンドでは動作しない** ✗

### 調査で排除した原因
- ✗ Cloud Functionsの問題 → ログで「Successfully sent alarm」を確認
- ✗ FCM送信の問題 → 通知は正常に届いている
- ✗ iOS通知設定 → 全てオン
- ✗ サイレントスイッチ → オフ
- ✗ `setBackgroundMessageHandler` → 無効化しても改善せず
- ✗ FCMペイロード形式 → 複数パターン試行済み

### 根本原因（2026-04-15 特定）

**iOSのバックグラウンド制限により、アプリがバックグラウンドにある時はJavaScriptコードが実行されない**

現在のアーキテクチャ:
```
フォアグラウンド時:
  FCM通知受信 → setForegroundMessageHandler → alarmService.triggerAlarmFromFCM()
    → audioService.playAlarmSound() → アラーム音がループ再生 ✓

バックグラウンド時:
  FCM通知受信 → iOS が通知を表示（通知音 "default" が鳴る）
    → JSコード実行されない → audioService.playAlarmSound() 呼ばれない ✗
```

**コードの流れ**:
1. `alarmService.ts:282` - `triggerAlarmFromFCM()` → `handleAlarmTriggered()`
2. `alarmService.ts:186` - `audioService.playAlarmSound(customSound, true)`
3. `audioService.ts:36-95` - expo-audioで`アラーム音.m4a`をループ再生

**問題**: バックグラウンドでは上記のJSコードが実行されないため、アラーム音が鳴らない

### 解決策

**APNs通知のカスタムサウンドとして、アラーム音をバンドルに含め、FCMペイロードで指定する**

iOSではAPNs通知のサウンドは最大30秒まで指定可能。カスタムアラーム音をアプリにバンドルし、FCMペイロードの`sound`フィールドで指定することで、バックグラウンドでもアラーム音が鳴るようにする。

```javascript
// Firebase Functions - FCMペイロード
apns: {
  payload: {
    aps: {
      alert: { title: "起床時間です！", body: "..." },
      sound: "alarm.caf",  // カスタムアラーム音（30秒以内）
      "interruption-level": "time-sensitive",
    },
  },
},
```

### 実装タスク

1. **アラーム音ファイルの準備**
   - `アラーム音.m4a`を30秒以内にトリミング
   - CAF形式に変換（`alarm.caf`）

2. **iOSバンドルへの追加**
   - Expo config pluginでXcodeプロジェクトに追加
   - または手動で`ios/okiroya/`に配置

3. **FCMペイロードの更新**
   - `sound: "alarm.caf"`に変更

4. **ユーザー起動時のアラーム継続**
   - 通知タップでアプリ起動 → ループアラーム再生開始
   - スクワット完了まで継続

---

## Problem 1: アラームが鳴らない (2026-04-09)

### 症状
- アプリ起動時もバックグラウンド状態でもアラームが鳴らない
- Xcodeコンソールにはアプリのライフサイクルイベント（appBecomesActive, appEntersBackground等）は表示されるが、アラーム関連のエラーは見られない

### 発見された原因

#### 1. Critical Alert エンタイトルメント不足 [重大度: 高]
- **ファイル**: `ios/app/app.entitlements`, `app.config.ts`
- **問題**:
  - `notificationService.ts` で `critical: true` を使用している（L191, L251）
  - しかし `ios/app/app.entitlements` に `com.apple.developer.usernotifications.critical-alerts` が**設定されていない**
  - `app.config.ts` の `entitlements` にも設定されていない
- **影響**: iOSでCritical Alert（サイレントモード・おやすみモードを無視して鳴る通知）が動作しない

#### 2. Notifeeプラグインがapp.config.tsに未登録 [重大度: 中]
- **ファイル**: `app.config.ts`
- **問題**: `plugins` 配列に `@notifee/react-native` が含まれていない
- **影響**: Expo prebuildでNotifeeのネイティブ設定が正しく適用されない可能性

#### 3. デフォルトアラーム音ファイル（alarm.caf）のバンドル未確認 [重大度: 中]
- **ファイル**: `ios/alarm.caf`, `functions/src/index.ts`
- **問題**:
  - FCM通知で `sound: "alarm.caf"` を指定している（L79）
  - `ios/alarm.caf` が追加されているが、ビルドに正しく含まれているか未確認
  - ※これはアプリにバンドルされたデフォルト音であり、ユーザー選択機能ではない
- **影響**: 通知のアラーム音が再生されない

#### 4. FCMトークン保存状況の未確認 [重大度: 要確認]
- **ファイル**: `src/services/fcmService.ts`
- **問題**: コードは正しく実装されているが、実際にFirestoreにトークンが保存されているか未確認
- **確認方法**: Firebase Console > Firestore > users/{userId}/fcmToken を確認

#### 5. Firebase Functions デプロイ状況の未確認 [重大度: 要確認]
- **ファイル**: `functions/src/index.ts`
- **問題**: 最新のコードがデプロイされているか、Cloud Schedulerが正しく実行されているか未確認
- **確認方法**: Firebase Console > Functions > ログを確認

#### 6. APNs設定の未確認 [重大度: 要確認]
- **問題**: Firebase ConsoleにAPNs認証キー（.p8）が正しく設定されているか未確認
- **確認方法**: Firebase Console > プロジェクト設定 > Cloud Messaging > Apple アプリの構成

### 関連するコードフロー
```
アラーム時間到達
    ↓
[Firebase Functions] checkAlarms が毎分実行
    ├─ Firestore で settings.alarmTime と現在時刻を比較
    ├─ 対象ユーザーにFCM通知を送信
    └─
[クライアント] FCM通知受信
    ├─ fcmService.ts でハンドリング
    ├─ alarmService.handleAlarmTriggered()
    ├─ audioService.playAlarmSound()
    └─ UI更新（スクワット画面表示）
```

---

## Problem 2: バックグラウンドでアラーム音が鳴らない (2026-04-09 15:00頃)

### 症状
- バックグラウンド状態で通知は来るがアラーム音が鳴らない
- Xcodeのコンソールにログが出力されない（JSコードが実行されていない）

### 発見された原因

#### 1. FCM通知のサウンド設定がバンドルに存在しない [重大度: 高]
- **ファイル**: `functions/src/index.ts`
- **問題**:
  - FCM通知で `sound: "alarm.caf"` が指定されている（デプロイ済みの古いコード）
  - しかし `alarm.caf` がアプリバンドルに含まれていない
  - iOSは存在しないサウンドファイルを再生できない
- **影響**: 通知は表示されるが音が鳴らない

#### 2. Firebase Functionsのデプロイ失敗 [重大度: 高]
- **問題**:
  - `sound: "default"` への変更をデプロイできていない
  - Cloud Buildで「An unexpected error occurred」エラーが発生
  - 古いコード（sound: "alarm.caf"）が本番で動作中
- **確認方法**: Google Cloud Console > Cloud Build でエラーログを確認

#### 3. prebuild時にカスタムファイルが消える [重大度: 中]
- **問題**:
  - `npx expo prebuild --clean` で `ios/` フォルダが再生成される
  - 手動で追加した `alarm.caf` が消える
- **影響**: prebuildのたびにファイルを再配置する必要がある

#### 4. バックグラウンドでJSコードが実行されない [重大度: 情報]
- **問題**:
  - iOSではバックグラウンドでJavaScriptランタイムが停止
  - `setBackgroundMessageHandler` 内のコードは実行されない可能性が高い
- **対策**: FCM通知のサウンド設定に依存する（JSに依存しない）

### 根本原因の特定

**バックグラウンドでアラーム音が鳴らない直接の原因**:
- FCM通知のペイロードで指定された `alarm.caf` がアプリバンドルに存在しない
- iOSは存在しないサウンドファイルを無視し、無音で通知を表示する

**解決策の優先順位**:
1. Firebase Functionsをデプロイして `sound: "default"` を適用（最も簡単）
2. または `alarm.caf` をアプリバンドルに正しく含める（Expo config plugin必要）

---

## Problem 5: Firebase FunctionsがFirestoreにアクセスできない (2026-04-10 21:17)

### 症状
- Firebase Functionsのログで`PERMISSION_DENIED: Missing or insufficient permissions`エラーが発生
- checkAlarms関数がFirestoreからユーザーデータを取得できない
- 結果として**FCM通知が一切送信されていない**

### エラーメッセージ
```
Error: 7 PERMISSION_DENIED: Missing or insufficient permissions.
```

### 発見された原因

#### 1. サービスアカウントのFirestore権限不足 [重大度: 致命的]
- **問題**:
  - Cloud Functionsが使用するサービスアカウント `okiroya-9af3f@appspot.gserviceaccount.com` に
    Firestoreへのアクセス権限がない
  - Firebase Admin SDKはデフォルトでこのサービスアカウントを使用する
- **影響**: **アラーム機能が完全に動作しない**

### 根本原因の特定

**これがアラームが鳴らない真の根本原因**:
1. checkAlarms関数が毎分実行される
2. Firestoreにクエリを実行しようとする
3. `PERMISSION_DENIED`エラーで失敗
4. ユーザーデータを取得できないため、FCM通知が送信されない
5. 結果として、通知は届かない（または音が鳴らない以前の問題）

**解決策**:
1. Google Cloud Console > IAM で、サービスアカウントに「Cloud Datastore User」ロールを付与
2. または「Firebase Admin SDK Administrator Service Agent」ロールを付与

---

## Problem 4: バックグラウンドで通知音が鳴らない（デプロイ後も継続）(2026-04-10)

### 症状
- Firebase Functionsデプロイ成功後もバックグラウンドで通知音が鳴らない
- 通知自体は正常に表示される
- Xcodeコンソールには`appEntersBackground`イベントが表示されている

### 調査結果

#### 確認済みの設定
- `ios/app/Info.plist`: UIBackgroundModes に `remote-notification`, `audio`, `fetch` が設定済み ✓
- `app.config.ts`: infoPlist に UIBackgroundModes が設定済み ✓
- `functions/src/index.ts`: `sound: "default"` に変更済み・デプロイ済み ✓
- `notificationService.ts`: `interruptionLevel: 'timeSensitive'` が設定済み ✓

#### 根本原因の特定

**原因1: iOSのサイレントスイッチ（物理スイッチ）[重大度: 致命的・可能性高]**
- iPhoneの側面の物理スイッチがオレンジ色（サイレントモード）の場合、通常の通知音は鳴らない
- `sound: "default"` はサイレントスイッチを**無視しない**
- `interruptionLevel: 'timeSensitive'` もサイレントスイッチを**無視しない**
- **サイレントスイッチを無視して音を鳴らすには Critical Alert が必要**（Apple特別承認必要）

**原因2: iOS通知設定でサウンドがオフ [重大度: 高・可能性中]**
- 設定 > 通知 > オキロヤ > サウンドがオフになっている可能性
- ユーザーが意図せずオフにしている場合がある

**原因3: 着信音/通知音の音量がゼロ [重大度: 高・可能性中]**
- 設定 > サウンドと触覚 > 着信音と通知音がゼロになっている可能性

**原因4: バックグラウンドでJSコードが実行されない [重大度: 情報]**
- iOSではバックグラウンドでJavaScriptランタイムが停止
- `audioService.playAlarmSound()` はバックグラウンドでは実行されない
- → FCM通知の`sound`設定のみに依存する必要がある（現状はこれで正しい）

### 結論

**最も可能性が高い原因**: ユーザーのiPhoneがサイレントモード（物理スイッチがオレンジ色）

**解決策の選択肢**:
1. **ユーザーにサイレントモードをオフにしてもらう**（即座に解決・制限あり）
2. **Critical Alert エンタイトルメントを取得**（Apple承認必要・時間がかかる）
3. **アプリ内でユーザーに注意喚起**（サイレントモードでは音が鳴らない旨を表示）

---

## Problem 3: FCM認証エラー (2026-04-09 15:40頃)

### 症状
- Cloud FunctionsからFCM通知を送信しようとすると認証エラーが発生
- `testAlarm` APIを呼び出すと「Failed to send alarm notification」が返される

### エラーメッセージ
```
FirebaseMessagingError: Request is missing required authentication credential.
Expected OAuth 2 access token, login cookie or other valid authentication credential.
```

### 発見された原因

#### 1. Cloud Messaging APIの権限問題 [重大度: 致命的]
- **問題**: Firebase Admin SDKがFCM通知を送信するための認証情報がない
- **原因**: サービスアカウントにCloud Messaging APIへのアクセス権限がない可能性
- **影響**: **全てのFCM通知が送信できない** → アラームが鳴らない

### 根本原因の特定

**これがアラームが鳴らない真の根本原因**:
- Cloud FunctionsはFirestoreクエリでユーザーを見つけている（または見つけようとしている）
- しかし、FCM通知を送信する時点で認証エラーが発生
- 結果として、通知は一切送信されていない

**解決策**:
1. Google Cloud Console > APIs & Services > Cloud Messaging API を有効化
2. サービスアカウントに「Firebase Cloud Messaging Admin」ロールを付与
3. または、Firebase Admin SDKの初期化方法を確認

---

## Problem 6: checkAlarmsクエリがユーザーを見つけられない (2026-04-12)

### 症状
- Firebase Functionsのログで「No users found with alarm at this time」と表示される
- Firestoreには`settings.alarmTime`が正しく保存されているユーザーが存在する
- しかし、checkAlarmsのクエリが一致するユーザーを見つけられない
- 結果として、FCM通知が送信されない

### 調査結果

#### 確認済み
- Firestoreデータ: `settings.alarmTime: "21:41"`, `settings.alarmDays: [1,2,3,4,5,6,0]`, `fcmToken: 存在する` ✓
- Firebase Functionsのログ: 毎分実行されている ✓
- サービスアカウント権限: Cloud Datastore User追加済み ✓

### 可能性のある原因

#### 1. Firestoreインデックスの問題 [重大度: 高・可能性: 中]
- **問題**: `settings.alarmTime`のようなネストされたフィールドへのクエリにインデックスが必要な場合がある
- **確認方法**: Firebase Console > Firestore > インデックス
- **対応**: 必要であればインデックスを作成

#### 2. データ構造の不一致 [重大度: 高・可能性: 高]
- **問題**: Firestoreの実際のデータ構造と、クエリが期待する構造が異なる可能性
- **確認方法**: Firebase ConsoleでRAWデータを確認
- **対応**: デバッグログで全ユーザーのデータを出力して確認

#### 3. FCMトークンの無効化 [重大度: 中・可能性: 中]
- **問題**: iOSではアプリを長期間使用しないとトークンが無効になる
- **確認方法**: アプリを再起動してFCMトークンを更新
- **対応**: アプリを再インストールしてトークンを再取得

#### 4. サービスアカウント権限の追加不足 [重大度: 高・可能性: 中]
- **問題**: `385341847803-compute@developer.gserviceaccount.com`にCloud Datastore Userがない
- **確認方法**: IAMで権限を確認
- **対応**: 追加でCloud Datastore Userロールを付与

#### 5. Cloud Functionsのコールドスタート問題 [重大度: 低・可能性: 低]
- **問題**: 新しいコードがまだ反映されていない
- **確認方法**: デバッグログが出力されるか確認
- **対応**: 時間を置いて再確認

---

## Problem 7: FCM認証エラー（再発）(2026-04-12)

### 症状
- testAlarm APIを呼び出すと「Failed to send alarm notification」が返される
- Firebase Functionsのログで`FirebaseMessagingError: Request is missing required authentication credential`が表示される

### エラーメッセージ
```
FirebaseMessagingError: Request is missing required authentication credential.
Expected OAuth 2 access token, login cookie or other valid authentication credential.
code: 'messaging/third-party-auth-error'
```

### 発見された原因

#### 1. サービスアカウントのFCM権限不足 [重大度: 致命的]
- **問題**: `okiroya-9af3f@appspot.gserviceaccount.com`にFCM APIへのアクセス権限がない
- **影響**: **全てのFCM通知が送信できない** → アラームが鳴らない

### 解決策
Google Cloud Console > IAMで、以下のサービスアカウントに権限を追加:
- サービスアカウント: `okiroya-9af3f@appspot.gserviceaccount.com`
- 追加するロール:
  1. 「Firebase Cloud Messaging API 管理者」(Firebase Cloud Messaging Admin)
  2. 「サービスアカウント トークン作成者」(Service Account Token Creator)

---

---

## Problem 10: バックグラウンド復帰・アプリ終了後起動でスクワット画面が表示されない (2026-04-16) [解決済み 2026-04-18]

### 症状
1. **バックグラウンドから復帰**: アラーム5分以内にバックグラウンドから復帰してもスクワット画面が表示されない
2. **アプリ終了後に起動**: アラームが鳴った後、アプリを終了状態から起動してもスクワット画面が表示されない

### 正常に動作するケース
- 通知タップでアプリを開く → スクワット画面が表示される ✓
- アプリアイコンタップで開く（通知無視）→ スクワット画面が表示される ✓

### 根本原因（2026-04-18 特定）

**二重AppStateリスナーと競合状態**

1. **二重リスナー問題**:
   - `alarmService.ts` (line 121-126): 内部で`AppState.addEventListener`を設定
   - `App.tsx` (line 90-120): 独自にも`AppState.addEventListener`を設定
   - 同じイベントに対して2つのリスナーが存在

2. **競合フロー**:
   ```
   AppState → 'active'
       ↓
   alarmService.handleAppStateChange() [リスナー1]
       ↓ checkAndStartAlarmIfNeeded()
       ↓ handleAlarmTriggered()
       ↓ this.alarmState = 'ringing'
       ↓ this.onAlarmTriggeredCallback() → setIsAlarmRinging(true)
       ↓
   App.tsx handleAppStateChange() [リスナー2]
       ↓ alarmService.checkAndStartAlarmIfNeeded()
       ↓ handleAlarmTriggered() → "Already ringing, skipping" (line 168-171)
       ↓ コールバック呼び出しがスキップされる
   ```

3. **コールバックStale Closure問題**:
   - `alarmService.setOnAlarmTriggered()` で設定されるコールバックは初期化時の参照
   - React状態の更新がバッチ処理で遅延される可能性
   - コールバック内の`setIsAlarmRinging`がstale closureの影響を受ける可能性

### 解決策

1. **alarmServiceの内部AppStateリスナーを削除**: App.tsxで一元管理
2. **handleAlarmTriggeredの改善**: ringing状態でもコールバックを呼び出す（UI同期のため）
3. **App.tsxのAppStateリスナーを主要な制御ポイントに**: 直接状態更新を行い、コールバックへの依存を減らす
4. **競合防止**: alarmServiceからcheckAlarmWindow()を呼び出し、App.tsx側でsetIsAlarmRinging(true)を直接設定

### 実装内容（2026-04-18）

**alarmService.ts**:
- `setupAppStateListener()` を削除（App.tsxに一元化）
- `handleAppStateChange` を削除
- `handleAlarmTriggered()` で `alarmState === 'ringing'` の場合もコールバックを呼び出すよう変更
- `cleanup()` からAppStateリスナーの削除処理を除去

**App.tsx**:
- `handleAppStateChange` で `checkAlarmWindow()` を直接呼び出し（checkAndStartAlarmIfNeeded()ではなく）
- ウィンドウ内なら `triggerAlarmFromFCM()` でアラーム音を開始し、`setIsAlarmRinging(true)` を直接設定
- 初期化時とlaunchチェック時も同様のパターンに変更

---

## Problem 11: サーバー側でX自動投稿ができない（トークン未保存問題）(2026-04-18)

### 症状
- スクワット未完了でも5分後にXへの自動投稿が行われない
- Firebase Functionsログで「X not connected, skipping penalty post」と表示される

### 根本原因

**XトークンがFirestoreに保存されておらず、サーバーからアクセスできない**

**useXAuth.ts (line 72-78)**:
```typescript
// Save connection to Firestore (without storing tokens in Firestore for security)
const firestoreConnection: SNSConnection = {
  connected: true,
  accessToken: null, // Don't store in Firestore ← 問題
  refreshToken: null, // Don't store in Firestore ← 問題
  connectedAt: result.connection.connectedAt,
  username: result.connection.username,
};
```

**現在のアーキテクチャ**:
```
クライアント側:
  X OAuth認証 → トークン取得
      ↓
  expo-secure-store に保存（セキュア、クライアントのみアクセス可能）
      ↓
  Firestore には connected: true, accessToken: null として保存

サーバー側:
  checkSquatCompletion 実行
      ↓
  Firestore から snsConnections.x を取得
      ↓
  accessToken が null → 「X not connected」と判定
      ↓
  投稿スキップ
```

### 付随する問題: タイムラグ

現在の実装では、checkSquatCompletionは毎分実行され「5分前±1分」のユーザーをチェック。
そのため、最大6分、最小4分のタイムラグが発生する可能性がある。
（ユーザー要望: 1分以内）

### 解決策

**Phase 1: Firestoreにトークンを保存するよう変更**
1. `useXAuth.ts`: X連携時にaccessToken/refreshTokenをFirestoreに保存
2. `functions/src/index.ts`: 既存のpostPenaltyTweetForUserはそのまま動作する

**Phase 2: タイムラグを1分以内に改善**
- 現在: 5分前±1分をチェック → 4〜6分のタイムラグ
- 改善: 5分前〜5分30秒前をチェック → 最大30秒のタイムラグ

### セキュリティ考慮事項
- Firestoreにトークンを保存する場合、Firestore Security Rulesで適切に保護する必要がある
- 現在のルールで`snsConnections`フィールドはユーザー本人のみ読み書き可能であれば問題なし

### 確認が必要な情報
- Firestoreセキュリティルール: Firebase Console > Firestore > ルール
- 現在のユーザードキュメント構造: Firebase Console > Firestore > users/{userId}

